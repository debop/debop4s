slick {

  database {
    host = "127.0.0.1:3306"
    name = "slick"

    driverClass = "org.mariadb.jdbc.Driver"
    url = "jdbc:mariadb://"${slick.database.host}"/"${slick.database.name}
    username = "root"
    password = "root"

    maxPoolSize = 64
    minIdleSize = 4
  }

}
