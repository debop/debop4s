slick {

  database {
    host = "localhost"
    name = "slick"

    driverClass = "org.postgresql.Driver"
    url = "jdbc:postgresql://"${slick.database.host}"/"${slick.database.name}
    username = "root"
    password = "root"

    maxPoolSize = 64
    minIdleSize = 4
  }

}