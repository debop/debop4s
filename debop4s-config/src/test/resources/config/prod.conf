include "local.conf"

application {

  database {
    host = "110.93.135.105"
    name = "healthconnect_production"
    username = "healthconnect"
    password = "rhrltkwntpdy"
  }

  hibernate {
    cacheProviderConfig = "hibernate-redis-redis.conf"
  }
}