slick {

  database {
    host = "127.0.0.1:3306"
    name = "northwind"
    driverClass = "com.mysql.jdbc.Driver"
    url = "jdbc:mysql://"${slick.database.host}"/"${slick.database.name}
    username = "root"
    password = "root"

    maxPoolSize = 64
    minIdleSize = 4
  }
}
