develop {
  db {
    default {
      driver = "org.h2.Driver"
      url = "jdbc:h2:mem:db"
      user = "sa"
      password = ""
    }

    legacy {
      driver = "org.h2.Driver"
      url = "jdbc:h2:mem:db"
      user = "foo"
      password = "bar"
    }
  }
}